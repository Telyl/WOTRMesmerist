using CharacterOptionsPlus.Util;
using Kingmaker.UnitLogic.Abilities.Components;
using Microsoft.Build.Utilities;
using TabletopTweaks.Core.MechanicsChanges;
using static TabletopTweaks.Core.MechanicsChanges.AdditionalActivatableAbilityGroups;

namespace Mesmerist.Utils
{
    public class MesmeristGroups
    {
        private static readonly Logging.Logger Logger = Logging.GetLogger(nameof(MesmeristGroups));

       //public static ExtentedActivatableAbilityGroup Stares = (ExtentedActivatableAbilityGroup)1818;
        //public static ExtentedActivatableAbilityGroup Tricks = (ExtentedActivatableAbilityGroup)1819;
    }
}
